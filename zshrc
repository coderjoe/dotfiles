. ~/.zsh/config
. ~/.zsh/aliases
. ~/.zsh/completion
. ~/.zsh/prompt
. ~/.zsh/dircolors
. ~/.zsh/nvm
. ~/.zsh/rvm
. ~/.zsh/rbenv

# use .localrc for settings specific to one system
[[ -f ~/.localrc ]] && . ~/.localrc
